package scores;

public interface AverageScoreStrategy {
    /**
     * method to calculate the averageScore of a person
     * in function of the category he's in by age
     * @return averageScore
     */
    Double getAverageScore();
}
